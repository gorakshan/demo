pipeline{
    agent any
    
    environment{
        PATH = "/opt/maven/bin:$PATH"
    }
    stages{
        stage("Git Checkout"){
            steps{
                echo "welcome to jenkins"
            }
        }
        stage("Maven Build"){
            steps{
                sh "mvn clean package"
            }
    }
}
}

